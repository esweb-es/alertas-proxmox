<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirigir de manera interna y transparente todas las peticiones a la carpeta /public
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
